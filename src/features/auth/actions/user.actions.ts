"use server";

/**
 * Session is available via getSession() from @/core/auth/session.
 * Full user (for server components) via getCurrentUser() from @/core/auth/current-user.
 * Client-side: use GET /api/auth/me for current user.
 */
