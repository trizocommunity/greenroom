export const systemConfig = {
  // Flag to enable the new Payment-First flow for Festival and Edition creation.
  // When false (default), the legacy flow is active.
  // When true, the new strict payment-first flow is enforced.
  paymentFirstFlowEnabled: false,
};
