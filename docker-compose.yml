version: "3.9"

services:
  db:
    image: postgres:16
    container_name: greenroom-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: greenroom
      POSTGRES_PASSWORD: greenroom
      POSTGRES_DB: greenroom
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

